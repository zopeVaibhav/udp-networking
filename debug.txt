ReadFromUDP: read udp [::]:9000: wsarecvfrom: A message sent on a datagram socket was larger than the internal message buffer or some other network limit, or the buffer used to receive a datagram into was smaller than the datagram itself.

if rand.Intn(100) < 20 {
      data[rand.Intn(len(data))] = byte(rand.Intn(256))
      fmt.Println("CORRUPTED packet from", clientAddr)
  }